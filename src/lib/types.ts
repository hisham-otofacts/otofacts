export interface CountryConfig {
  code: string;
  name: string;
  currency: string;
  language: string;
}

export interface HeadProps {
  title: string;
}

export interface Session {
  id: string;
}
